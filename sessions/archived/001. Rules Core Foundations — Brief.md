# Session 001 — Rules Core Foundations — Brief

**Type:** feature
**Shape:** plan-backed
**Branch:** session-001

## Goal

Build the pure, headless foundation of the rules engine: the board/territory model,
elevation with the smoothness constraint, terrain editing and land reclamation, the
Supply/Energy economy, and structure placement — all in `src/shared/`, with zero Roblox API
calls, tested under Lune.

## Plan reference

This session executes the plan file's entry for **Rules core: board, terrain, and economy**
(`sessions/plan.md`). Read that entry first — it is canonical for scope, success criterion,
prerequisites, and artifact.

## Reading list

Read these before touching anything, in order:

1. The plan file's entry for **Rules core: board, terrain, and economy**.
2. This brief.
3. The previous session's log, `sessions/000. Kickoff — Log.md` — "Notes for next session."
4. `sessions/proposal.md`, sections 2 (tech stack, repo layout), 3 (board, grid, elevation),
   7 (resources), 8 (structures), and 9 (terrain editing and expansion) — the source spec for
   everything this session builds. No project code exists yet, so there are no other files to
   read first.

## Starting state

- The repo is git-initialized with the session-kit scaffolding and the proposal/plan docs
  committed; no code exists yet anywhere in `src/`, `sim/`, or `tests/`.
- Rojo and Lune aren't wired up yet — this session is the first to need them, so setting up
  `default.project.json` and the Lune test entry point is part of this session's own scope,
  not a prerequisite someone else did.
- The housekeeping inbox is empty; nothing to absorb this session.

## Out of scope

Line-of-flight, volley resolution, fog/belief maps, unlock gates, and the victory conditions
— all of that is the next plan entry, **Rules core: combat, fog, and victory**, which depends
on this one.

---

## Process rules

Follow the `session-protocol` skill's process rules in full. Restated here because they
govern the work directly:

- A question from the user is a question, not an instruction to act. Answer it, then wait.
- Read every file you intend to modify before editing it.
- Adapt silently to small drift between this brief and the code; note the adaptation in one
  line. Pause and ask only for decisions that are genuinely expensive to reverse — data
  shape, new abstractions, anything cross-cutting. If a reasonable reading of the surrounding
  code lands on the same answer, just decide.
- When implementation is complete, run `test-command` (from `sessions/config.md`) and fix
  failures before announcing completion. If there's no `test-command`, follow `verify-step`
  instead. Neither is set yet in this project's config — `lune run test` is the de facto
  check per the proposal; treat it as this session's verification and consider proposing it
  as `test-command` in config once it exists.
- Verify objective outcomes yourself — don't hand the user something to run and report back
  when you can observe the result directly. Pull the user in only for judgment only a human
  can make: visual design, UX, whether an interaction feels right.
- Match existing code conventions. No comments or type annotations on code you didn't write.

## Session Open Gate

After the reading list, starting state, and process rules are read — and before any task
work — post a short orientation: what shape this session is, the work plan, anything that
needs clarifying. Then wait for the user to confirm. Do not pipeline from reading into
implementation.

## Session Close Gate

Everything below happens only when the user runs `/close-session`. See the `session-protocol`
skill for the full Close Gate steps; this brief adds no session-specific steps beyond those.
