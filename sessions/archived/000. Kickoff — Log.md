# Session 000 — Kickoff — Log

**Date:** 2026-09-17
**Branch:** session-000
**Type:** planning

## What was built

### Phase 1 — Read the proposal

- Read `sessions/proposal.md` (the full *Island Siege* build spec), the empty plan-file
  skeleton, and `sessions/config.md`.

### Phase 2 — Drafted the plan

- Turned the proposal's milestone sequence into ten entries in `sessions/plan.md`, each with
  status, prerequisites, a success criterion, and an artifact:
  1. Rules core: board, terrain, and economy
  2. Rules core: combat, fog, and victory
  3. Simulation harness and first balance sweep
  4. Board in Studio
  5. Planning phase
  6. Resolve phase vs. a dummy opponent
  7. Full match loop and wrapper screens
  8. Real two-player networking
  9. Bot opponent
  10. Ship polish and balance pass
- Split the proposal's single "rules core" milestone into two entries (foundations vs.
  combat/fog/victory) given its size and the exhaustive line-of-flight and victory-condition
  test requirements. Kept every other milestone as one entry apiece, per the project's bias
  toward bigger sessions.
- Ordered the simulation harness right after the combat/fog/victory entry (matches the
  proposal's own M1.5 placement) so balance sweeps can start before any UI exists.

## Verification

No `test-command` or `verify-step` is set in `sessions/config.md`, and none applies to a
planning session — this session's artifact is the plan file itself, not code. No automated
check was run.

## Deferred / carried forward

- Nothing deferred from this session's own scope. The proposal's own open design questions
  (round-timer length, round limit, point values, grace-window scaling, theming, rubble
  persistence, terrain-vs-plot invalidation, mobile input) are explicitly deferred by the
  proposal to a running `DECISIONS.md` during implementation, not to plan-time decisions —
  they aren't blocking and weren't promoted to plan entries or open questions.

## Notes for next session

- This is the project's first session: the repo was just initialized under git (branch
  `master` had no commits yet); this session's work is the first commit, on branch
  `session-000`.
- No code exists yet anywhere in `src/`, `sim/`, or `tests/` — the next session starts from
  an empty repo aside from the session-kit scaffolding and the proposal/plan docs.
- The next session is plan-backed against the first entry, **Rules core: board, terrain, and
  economy** — no prerequisites, so it can start immediately.
